DirectoryIndex index.php home.php

<IfModule mod_rewrite.c>
    RewriteEngine On

    # Only rewrite if the file or directory doesn't exist
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [QSA,L]
</IfModule>